\# CLAUDE.md



Use `AGENTS.md` in the repository root as the primary shared instruction source for this project.



Repository-specific emphasis:

\- Separate `Run now` commands from `Do not run now / example` commands.

\- Do not suggest release-finalization or branch-changing commands until the repository state has been checked and the user explicitly wants that action now.

